name: Codex Feature Developer

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Feature branch to implement"
        required: true
        type: string
      plan_path:
        description: "Approved task plan file"
        required: true
        type: string
      model:
        description: "Catalog-approved OpenAI model"
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: codex-feature-developer-${{ inputs.branch }}
  cancel-in-progress: false

jobs:
  develop:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check OpenAI API key
        run: |
          if [ -z "${{ secrets.OPENAI_API_KEY }}" ]; then
            echo "OPENAI_API_KEY is not configured for this repository." >&2
            exit 1
          fi

      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}
          fetch-depth: 0

      - name: Verify approved plan exists
        run: |
          test -f "${{ inputs.plan_path }}" || { echo "Plan file not found." >&2; exit 1; }
          grep -Eq "Status: (READY_FOR_DEVELOPMENT|READY_FOR_RETRY|READY_FOR_CORRECTION)" "${{ inputs.plan_path }}" || {
            echo "Plan must be READY_FOR_DEVELOPMENT, READY_FOR_RETRY, or READY_FOR_CORRECTION." >&2
            exit 1
          }

      - name: Mark developer stage active
        env:
          PLAN_PATH: ${{ inputs.plan_path }}
        run: |
          python - <<'PY'
          from pathlib import Path
          import os
          p = Path(os.environ["PLAN_PATH"])
          t = p.read_text()
          if "Status: READY_FOR_DEVELOPMENT" in t:
              t = t.replace("Status: READY_FOR_DEVELOPMENT", "Status: DEVELOPING", 1)
          elif "Status: READY_FOR_RETRY" in t:
              t = t.replace("Status: READY_FOR_RETRY", "Status: DEVELOPING", 1)
          elif "Status: READY_FOR_CORRECTION" in t:
              t = t.replace("Status: READY_FOR_CORRECTION", "Status: DEVELOPING", 1)
          else:
              raise SystemExit("Task is not startable.")
          p.write_text(t)
          PY

      - name: Ask Codex Developer to implement approved plan
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          model: ${{ inputs.model }}
          effort: low
          permission-profile: ":workspace"
          prompt: |
            You are the Developer for ${{ github.repository }}.
            Read AI_POLICY.md, .ai/BRANCH_OWNERSHIP.md, AGENTS.md, PRODUCT.md, ARCHITECTURE.md, .ai/roles/DEVELOPER.md, and ${{ inputs.plan_path }}.
            Implement ONLY the approved plan. Follow its approved architecture and dependency decisions exactly.
            Do not add auth, payments, databases, external APIs, paid services, deployment changes, or unapproved dependencies/frameworks.
            Add deterministic tests for changed behavior. Provide or update .factory/test.sh so CI can install only the project's required test tooling and run those tests deterministically. Keep that script project-specific and non-interactive. Do not commit, push, merge, create a PR, or install tools outside the repository. Stop after editing the implementation and tests.

      - name: Ensure Codex made changes
        run: |
          if git diff --quiet; then echo "Codex made no changes." >&2; exit 1; fi
          git status --short

      - name: Preserve generated implementation before validation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Preserve Codex implementation pending validation"
          git push origin HEAD:"${{ inputs.branch }}"

      - name: Run deterministic validation
        shell: bash
        run: |
          set -euo pipefail
          test -f .factory/test.sh || { echo "Implementation must provide .factory/test.sh as its deterministic test contract." >&2; exit 1; }
          chmod +x .factory/test.sh
          ./.factory/test.sh

      - name: Advance plan status
        env:
          PLAN_PATH: ${{ inputs.plan_path }}
        run: |
          python - <<'PY'
          from pathlib import Path
          import os
          p = Path(os.environ["PLAN_PATH"])
          t = p.read_text()
          if "Status: DEVELOPING" not in t: raise SystemExit("Expected DEVELOPING status.")
          p.write_text(t.replace("Status: DEVELOPING", "Status: READY_FOR_REVIEW", 1))
          PY

      - name: Mark validated implementation ready for review
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "${{ inputs.plan_path }}"
          git commit -m "Mark validated implementation ready for review"
          git push origin HEAD:"${{ inputs.branch }}"

      - name: Pause task if developer stage fails
        if: failure()
        env:
          PLAN_PATH: ${{ inputs.plan_path }}
        run: |
          if [ ! -f "$PLAN_PATH" ]; then exit 0; fi
          python - <<'PY'
          from pathlib import Path
          import os
          p = Path(os.environ["PLAN_PATH"])
          t = p.read_text()
          if "Status: DEVELOPING" in t: p.write_text(t.replace("Status: DEVELOPING", "Status: PAUSED_AI_FAILURE", 1))
          PY
          if git diff --quiet -- "$PLAN_PATH"; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$PLAN_PATH"
          git commit -m "Pause AI task after failed developer run"
          git push origin HEAD:"${{ inputs.branch }}" || true
